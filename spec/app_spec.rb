require_relative '../lib/app'
describe "crypo_scrapper" do
  it "" do
    expect(crypo_scrapper).not_to be_nil
  end
end


=begin
	
arriver à tester 
1) le fonctionnement de base de ton programme 
(pas d'erreur ni de retour vide)  
2) que ton programme sort bien un array cohérent 
(vérifier la présence de 2-3 cryptomonnaies, vérifier que l’array est de taille cohérente, etc.).

#autant d'entrée dans le array que de crypto sur le site
#hash avec BTC en key et un float non nul, non nil en value
	
end